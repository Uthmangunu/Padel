services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: padel
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5433:5432"]
    volumes: ["padel_postgres:/var/lib/postgresql/data"]
volumes:
  padel_postgres:
